<div class="co_container">
  <!-- ----------- Order Items Start ------------ -->
  <div class="co_content">
    <%= simple_form_for [:admin, @product] do |f| %>

      <div class="group">
        <%= f.input :title %>
      </div>

      <div class="group">
        <%= f.input :description %>
      </div>

      <div class="group">
        <%= f.input :quantity %>
      </div>

      <div class="group">
        <%= f.input :price %>
      </div>

      <div class="form-group">
        <%= f.simple_fields_for :photo do |c| %>
          <%= c.input :image , as: :file %>
        <% end %>
      </div>

      <%= f.submit "Submit", class: "btn btn-primary btn-sm", data: { disable_with: "Submitting..." } %>

    <% end %>
  </div>
</div>


